<UserControl x:Class="PL.Controls.LabeledDatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Controls"
             mc:Ignorable="d" 
             Name="_this">

    <Grid Margin="0,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0"
               HorizontalAlignment="Left"
               VerticalAlignment="Center" 
               Content="{Binding ElementName=_this, Path=Label}" 
               Style="{StaticResource ConfigLabelStyle}"/>

        <DatePicker Grid.Column="1"
                    VerticalAlignment="Center"
                    SelectedDate="{Binding Date, ElementName=_this, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled="{Binding IsEnabled, ElementName=_this}"
                    Style="{Binding InputStyle, ElementName=_this}"/>
    </Grid>
</UserControl>