<UserControl x:Class="PL.Controls.LabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Controls"
             mc:Ignorable="d" 
             Name="_this">

    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" 
               Content="{Binding ElementName=_this, Path=Label}" 
               Style="{StaticResource ConfigLabelStyle}"/>

        <TextBox Grid.Column="1" 
                 Text="{Binding ElementName=_this, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 IsReadOnly="{Binding ElementName=_this, Path=IsReadOnly}"
                 Style="{Binding ElementName=_this, Path=InputStyle}"/>
    </Grid>
</UserControl>
