<UserControl x:Class="PL.Controls.TopBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800"
             x:Name="_this">

    <Border Background="{StaticResource DarkBlueBrush}" Height="40"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0"
       HorizontalAlignment="Left"
       VerticalAlignment="Center" 
       Height="25" Width="25" Margin="10,0,0,0"
       Source="/PL;component/Helpers/Image.ico" 
       RenderOptions.BitmapScalingMode="HighQuality"/>

            <Label Grid.Column="0" 
                   Content="{Binding ElementName=_this, Path=Title}" 
                   Style="{StaticResource StandardLabel}" 
                   HorizontalAlignment="Center"
                   FontSize="20" Margin="10,0"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Click="BtnClose_Click"
                        Style="{StaticResource BtnExit}"
                        ToolTip="Close"/>

                <Button Click="BtnBack_Click"
                        Style="{StaticResource BtnBack}"
                        Visibility="{Binding ElementName=_this, Path=BackButtonVisibility}"
                        ToolTip="Back"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
