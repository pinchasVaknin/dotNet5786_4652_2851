<UserControl x:Class="PL.Controls.LabeledComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PL.Controls"
             mc:Ignorable="d"
             Name="_this">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" 
               Content="{Binding ElementName=_this, Path=Label}" 
               Style="{StaticResource ConfigLabelStyle}"/>

        <ComboBox x:Name="cmbInner" 
                  Grid.Column="1"
                  ItemsSource="{Binding ElementName=_this, Path=ItemsSource}"
                  SelectedItem="{Binding ElementName=_this, Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Style="{Binding ElementName=_this, Path=InputStyle}"/>
    </Grid>
</UserControl>
